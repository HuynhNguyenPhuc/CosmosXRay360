# MedNeRF Change & Fix Log

## 2026-08-16 — Seeded Off-Axis Validation View Sampling

**Phase:** 2 (Training & Validation Parity)  
**Files:** `baselines/train/mednerf.py`  
**Change:** Updated `train_mednerf.py` validation loop to use `val_rng = np.random.RandomState(42)` for selecting deterministic off-axis validation target views (`val_pairs`) instead of hard-pinning index 0 ($0^\circ$).  
**Why:** Ensures validation loss (`avg_val_loss`) accurately measures off-axis novel-view fitting quality during training, matching SV-DRR's seeded validation protocol.  
**Verification:** All unit and regression tests pass cleanly.

---

## 2026-08-15 — Target Azimuth Fitting Support Across 93-View Cache

**Phase:** 2 (Training Protocol / View Generalization)  
**Files:** `baselines/models/mednerf.py`, `baselines/train/mednerf.py`  
**Change:** Updated `fit_latent_and_weights` to accept `azimuth: float = 0.0`. Updated `train_mednerf` to cache all views and angles and sample random target view angles `(target_xr, target_azimuth)` for per-scan fitting during training.  
**Why:** Extends MedNeRF fitting across the full 93-view sweep during training, preventing latent code fitting from overfitting to 0° frontal view alone.  
**Verification:** `python run_all_tests_isolate.py` passes all unit tests.

---

## 2026-08-15 — Reverted the Wrapper Default Back to 50; Made 500 an Explicit Opt-In Instead

**Phase:** 3 (Training/Inference Scope) -- corrects the entry directly below, same day
**Files:** `baselines/models/mednerf.py`, `scripts/regenerate_tb_images.py`
**Change:** The entry below raised `MedNeRFWrapper.__init__`'s `iterations` default from 50 to 500 to
fix a real quality problem, but that default is shared by *every* caller, not just the one
visualization script that wanted it -- including `baselines/evaluate.py`'s `UnifiedBaselineEvaluator`
(constructs `MedNeRFWrapper(ckpt_path)` with no override) and `baselines/tests/test_mednerf_wrapper.py`.
Running the unit test after the 500 change took 15 minutes (was ~1 minute) -- confirmed the class
default change would have made a full NSCLC benchmark run (402 patients) cost on the order of 100+
hours for MedNeRF alone, an unintended and unbudgeted consequence of what was meant to be a
visualization-only fix. Reverted the class default to 50. Added `--iterations` to
`scripts/regenerate_tb_images.py` (checks `inspect.signature(wrapper_cls.__init__)` and only forwards
the kwarg to wrappers that accept it, e.g. `naf`/`mednerf`, warning and ignoring it otherwise) so a
one-off high-quality panel is an explicit `--iterations 500` flag, not a silently-shared default.
**Why:** Decouples "safe fast default for automated harnesses" (tests, full benchmark runs) from
"deliberately slow high-quality for a one-off demo panel" -- the two use cases have very different cost
tolerances and shouldn't share one number.
**Verification:** `uv run pytest baselines/tests/test_mednerf_wrapper.py` back down to ~68s (was
910s/15min with the 500 default). Confirmed `regenerate_tb_images.py --iterations 500` still forwards
correctly (`inspect.signature` check verified directly against `MedNeRFWrapper`/`SVDRRWrapper`/
`NAFWrapper` -- accepts for the first and third, correctly detected as unsupported for the second).
`results/mednerf.png` unaffected -- it was generated by a real 500-iteration run before this revert and
still accurately reflects that.

---

## 2026-08-15 — Wrapper Default Per-Patient Fitting Iterations Raised 50 -> 500 (superseded, see above)

**Phase:** 3 (Training/Inference Scope)
**Files:** `baselines/models/mednerf.py`
**Change:** `MedNeRFWrapper.__init__`'s `iterations` default changed from 50 to 500.
**Why:** 50 was 10% of `fit_latent_and_weights`' own built-in default (500) -- a speed shortcut for
generating TensorBoard visualization panels quickly during training, not a considered inference
setting. At 50 iterations, `results/mednerf.png`'s near-frontal reconstruction was a low-quality blob;
at 500 (this fix), the same view shows a clearly recognizable ribcage/mediastinum silhouette matching
ground truth.
**Verification:** Regenerated the panel via `scripts/regenerate_tb_images.py --baseline mednerf` against
the real `mednerf_best.pt` checkpoint and a real NSCLC test patient -- visual improvement confirmed
directly, not assumed from the parameter change alone. Real cost: ~17.4 min/patient at 500 iterations
vs. ~60s at 50 (single-GPU, single-patient) -- a genuine tradeoff, not free.

---

## 2026-08-03 — Initial MedNeRF Integration & Value-Space Target Scaling Fix

**Phase:** 1 & 2 (Import & Correctness)  
**Files:** `baselines/models/mednerf.py`, `baselines/train/mednerf.py`, `baselines/cloned/mednerf/graf-main/submodules/torchsearchsorted.py`  
**Change:** Created `MedNeRFWrapper` and `train_mednerf` training script. Substituted PyTorch native `torch.searchsorted` for legacy C++ `torchsearchsorted` extension. Rescaled `target_xr` in `fit_latent_and_weights` from `[0, 1]` to `[-1, 1]` (`target_xr * 2.0 - 1.0`).  
**Why:** Fixes C++ extension compile failures on PyTorch 2.x/CUDA 12+ and eliminates value-space mismatch gradients (MedNeRF generator outputs `[-1, 1]` via `tanh`).  
**Verification:** Verified in `baselines/tests/test_loss_regression.py` (`test_mednerf_target_scaling`).

---

## 2026-08-05 — PAPER.md/CODE.md Corrected Against the Actual Cached PDF

**Phase:** 0 (Resource acquisition / correctness of docs, not code)
**Files:** `docs/baselines/mednerf/{PAPER.md,CODE.md}`
**Change:** The 2026-08-05 docs backfill pass used a slightly wrong paper title and, more
substantively, omitted the paper's actual stated contribution over GRAF (§II-C's self-supervised
auto-encoding discriminator + DAG multi-head training) while stating test-time loss coefficients
($\lambda_{\text{MSE}}=1.0$, $\lambda_z=0.01$) that don't match the paper's real reported values
(Eq. 10: $\lambda_1{=}0.3,\lambda_2{=}0.1,\lambda_3{=}0.3$, lr=0.0005, $\beta_1{=}0,\beta_2{=}0.999$).
Notably, **this project's own `fit_latent_and_weights` already uses the real paper's exact
coefficients** (see the 2026-08-05 entry below) — only the documentation had it wrong, not the code.
`CODE.md` was largely accurate but under-described the discriminator (called it a generic "PatchGAN"
when `SimpleDecoder`, confirmed real in `discriminator.py`, is literally the paper's own "SD: Simple
Decoder" self-supervision component from Fig. 2). Rewritten directly from
`arxiv_papers/MedNeRF_2202.01020.pdf`.
**Why:** Same failure mode as the other 2026-08-05 correction entries across `docs/baselines/`.
**Verification:** Title/authors/venue/equation numbers/coefficients cross-checked page-by-page
against the PDF; `Discriminator`/`SimpleDecoder` class names re-verified with `grep`; license
(MIT, copyright Jonathan Frawley — one of the paper's actual authors) verified against the real
`LICENSE` file, and was already correct.

---

## 2026-08-05 — Reference Optimizer Alignment, Gaussian Prior NLL & LPIPS Caching

**Phase:** 2 & 3 (Correctness, Acceleration & Convention Alignment)  
**Files:** `baselines/models/mednerf.py`, `baselines/train/mednerf.py`  
**Change:** Updated `z_optim` in `fit_latent_and_weights` to `Adam(lr=0.0005, betas=(0.0, 0.999))` and added Gaussian NLL prior penalty `0.3 * nll` on $z$, matching reference `render_xray_G_Z.py` exactly. Cached `lpips.PerceptualLoss` process-wide as `_PERCEPT_LOSS` and added `--val_iters_per_sample`. Switched target angle generation from `endpoint=False` to `endpoint=True`.  
**Why:** Aligns test-time latent fitting with `render_xray_G_Z.py`, prevents $z$ from drifting far from the standard-normal prior, eliminates AlexNet reload I/O overhead, and aligns azimuth angles with ground-truth DiffDRR projections (`views/*.png`).  
**Verification:** `MedNeRFWrapper` test-time fitting executes stably and azimuth indices match `views/*.png`.

---

## 2026-08-07 — Phase 3: Opt-In Mixed Precision for Training Only (Not Inference)

**Phase:** 3 (Acceleration)
**Files:** `baselines/models/mednerf.py`, `baselines/train/mednerf.py`
**Change:** Added `use_amp: bool = False` to `fit_latent_and_weights`, wrapping the forward pass
(generator + LPIPS) in `torch.autocast` with a `torch.amp.GradScaler` shared across both
`z_optim`/`g_optim` (`scaler.scale(loss).backward()` once, `scaler.step()` each optimizer,
one `scaler.update()`). Threaded through as `--amp` in `baselines/train/mednerf.py`, wired only to
the *training* fit call -- validation's internal trend-tracking fit and, critically,
`MedNeRFWrapper.infer_multi_views` (which produces this project's actual reported benchmark numbers)
are untouched and stay fp32 by default.
**Why:** Considered whether MedNeRF's per-scan fitting had the same `exp()`-overflow risk that caused
a real NaN bug in XRaySyn (see `docs/baselines/xraysyn/LOG.md`'s 2026-08-07 entry) before enabling
this. Checked the actual alpha-compositing formula this method uses
(`baselines/cloned/mednerf/graf-main/submodules/nerf_pytorch/run_nerf_mod.py:238`,
`1 - exp(-relu(raw)*dists)`): the exponent is always `<= 0` (density is ReLU'd non-negative, `dists`
is a positive ray-step distance), so `exp()` is bounded in `(0, 1]` and structurally cannot overflow
under reduced precision -- a fundamentally different, safer shape than XraySyn's unbounded
`exp(+atten_proj)`. This gave enough confidence to try AMP here, though the LPIPS/generator layers
themselves weren't individually audited for fp16 underflow, hence defaulting to off and requiring an
explicit `--amp` opt-in.
**Verification:** Local smoke test, 2 train / 1 val patient, `--iters_per_sample 20`: both fp32 and
`--amp` runs complete with real (non-`nan`, non-crashing) loss values in the same order of magnitude
(0.358/0.481 fp32 vs 0.435/0.508 with `--amp` -- not a same-seed apples-to-apples comparison, `z` is
randomly initialized each run, so some divergence is expected regardless of precision). Measured
wall-clock: 44s (fp32) -> 26s (`--amp`) for the identical workload, a real ~1.7x speedup. This is a
small-scale smoke test, not a full-dataset regression -- treat any benchmark number produced with
`--amp` as provisional until a real full-scale run confirms output quality isn't measurably affected.

---

## 2026-08-19 — Empirical MedNeRF Pose-Sign & Cross-Angle Reconstruction Multi-Patient Audit

**Phase:** 3 (Correctness & Diagnostic Audit)  
**Files:** `scripts/check_mednerf_pose_sign.py`, `baselines/models/mednerf.py`, `docs/baselines/mednerf/LOG.md`  
**Change:** Performed an empirical cross-angle reconstruction and pose-sign audit on MedNeRF (`MedNeRFWrapper`) across multiple test patients (`LUNG1-004_0000` and `LUNG1-001_0000`) at 10x standard fitting iterations (500 iterations vs 50 default).  
**Empirical Findings:**  
Across both independent test scans (seeded with seed 0 and seed 1), comparing MedNeRF generated views at $+\theta$ vs $-\theta$ against DiffDRR ground-truth projections yields an identical magnitude match/flip pattern:

| Azimuth ($\theta$) | LUNG1-004 (seed 0) | LUNG1-001 (seed 1) |
|---|---|---|
| 30° | MATCH (+$\theta$) | MATCH (+$\theta$) |
| 60° | MATCH (+$\theta$) | MATCH (+$\theta$) |
| 90° | FLIPPED ($-\theta$) | FLIPPED ($-\theta$) |
| 120° | FLIPPED ($-\theta$) | FLIPPED ($-\theta$) |
| 150° | FLIPPED ($-\theta$) | FLIPPED ($-\theta$) |
| 180° | TIE (GT(+180) == GT(-180)) | TIE (GT(+180) == GT(-180)) |

**Analysis & Conclusion:**  
- **Not a global sign bug:** A simple coordinate/sign inversion error in wrapper code would flip all angle magnitudes. Inverting the sign in `infer_multi_views` would merely swap which magnitudes match vs flip (making 30°/60° flip and 90°-150° match), preserving the exact same crossover phenomenon.
- **Single-view GAN fitting ceiling:** MedNeRF fits a per-scan latent code $z$ and generator weights via single-view optimization without 3D or multi-view supervision. The reconstruction stays directionally reliable near the frontal anchor ($\le 60^\circ$ away) where optimization is tightly constrained, but becomes directionally ambiguous and underdetermined at far-side angles ($\ge 90^\circ$).
- **Resolution:** MedNeRF's azimuth convention in `MedNeRFWrapper` is confirmed correct as implemented. The observed off-axis degradation is a fundamental reconstruction-fidelity limitation of MedNeRF's single-shot architecture rather than a fixable pose-sign bug in code.

